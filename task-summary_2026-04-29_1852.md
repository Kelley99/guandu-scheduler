# Task Summary: Flask+Vue 官渡工具渲染修复

## Objective
修复 Flask+Vue 官渡自动分配工具页面空白问题（Vue mount 成功但内容不渲染）

## Key Reasoning
1. 用户反馈7个 alert 弹窗顺序为 1,2,3,6,7,4,5 — 确认 Vue mount 成功，但页面仍空白
2. 顺序异常实际是正常的：1→2→3→6→7 是同步路径，4→5 是异步 init() 在 mount 后执行
3. Vue mount 成功但内容空白 = 模板渲染失败，而 Vue **prod 模式静默吞掉渲染错误**
4. 逐项检查 setup() return 与模板引用，发现 `getStatLabel` 函数定义了但未 return
5. 模板中 `{{ getStatLabel(name) }}` 调用 undefined → 渲染报错 → prod 模式无提示 → 空白

## Fixes Applied
1. **添加 `getStatLabel` 到 setup() return 对象** — 根因修复
2. **添加 `app.config.errorHandler`** — 捕获 Vue 渲染错误并弹 alert，以后不再静默吞错
3. 将 `createApp({...}).mount('#app')` 拆为 `const app = createApp({...})` → `app.config.errorHandler` → `app.mount('#app')`

## Conclusions
- 用户确认页面加载正确 ✅
- Vue prod 模式下模板引用未 return 的函数会导致渲染失败但零报错 — 极其隐蔽的 bug
- 以后 Vue Composition API 开发必须：return 前对照模板逐项检查 + 配置 errorHandler
