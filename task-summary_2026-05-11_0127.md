# 考勤战报：积分字段记录功能

## 需求
OCR 识别积分榜时，成员名字能识别出来，但功勋积分没有记录。

## 改动

### 数据库
- `attendance_detail` 表新增 `points INTEGER DEFAULT 0` 列
- `create_report` 端点新增 `ALTER TABLE IF NOT EXISTS` 兼容已有数据库

### 前端 OCR 解析（onOcrImage）
- 用 Map 合并多张截图的积分（同名累加）
- 正则解析：把行尾最后一个数字当作积分
- 格式：`rank name score` → `{name, points}`

### 前端显示
- 识别结果格式：`✅ 已识别 4 人（总积分 620）：张三(+200)、李四(+150)...`
- 战报详情表格：成员名后显示 `(+积分)` 金色标签
- 导出 Excel：points 字段包含在导出数据中

### 后端 create_report
- 接收 `ocr_data: [{name, points}]` 格式
- 兼容旧格式 `ocr_names: [str]`（向后兼容）
- 积分累加同名合并
- INSERT 语句加入 points 字段

## 文件
- `/Users/kelley/.qclaw/workspace-agent-29b6e205/app.py`
- `/Users/kelley/.qclaw/workspace-agent-29b6e205/templates/index.html`
