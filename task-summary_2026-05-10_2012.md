# 任务总结：考勤系统API + 前端重写

## 目标
重建官渡自动分配工具的考勤系统，从旧的"积分榜"模式升级为"考勤战报"模式。

## 完成内容

### 1. 数据库Schema（gc_attendance.db）
旧表 `attendance` + `attendance_log` → 5张新表：
- **members**: 成员名单（name唯一）
- **assignments**: 分配记录（section唯一）
- **assignment_members**: 分配明细（关联成员与分配记录）
- **attendance_reports**: 战报记录（section + date）
- **attendance_detail**: 考勤明细（成员+战报+状态）

### 2. 后端API（app.py）
12个新端点，替代旧的5个：
- 成员：list/import(add批量)/add(单个)/delete
- 分配记录：list/save/<id>/members
- 战报：list/create/delete
- 考勤明细：detail/<id>/update/batch_update
- 导出：export（密码lingxiao2026）

### 3. 前端（templates/index.html）
步骤4考勤管理完全重写，3个Tab：
- **成员名单**：从属性表导入/手动添加/删除
- **战报记录**：列表显示各状态人数/查看/导出/删除
- **创建战报**：选军团+日期+OCR截图识别出席成员→生成

考勤明细：四态（出席/缺席/请假/候补上场），支持实时修改状态。

### 4. 设计决策
- 成员来源：属性表导入（前端传names列表），未来支持战力表
- 考勤关联：创建战报时手动选军团+日期
- 缺席判定：基准名单有但OCR无=缺席，OCR有但基准无=候补上场
- 请假状态：创建后手动修改

### 验证结果
所有API端点测试通过：import→create_report→detail→update_status→export

### 文件修改
- app.py: 新schema + 12个API端点（替换旧5个）
- templates/index.html: 考勤Tab重写（1407行→73106字节）
- gc_attendance.db: 旧DB删除，新schema自动创建
