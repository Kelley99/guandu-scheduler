# 任务摘要 - axios 替换为 fetch

**时间**: 2026-04-26 21:00

## 目标
解决 Vue 前端 axios 请求挂起问题 - Promise 既不 resolve 也不 reject

## 根因
axios 从 CDN `unpkg.com` 加载，在某些网络环境下加载失败或极慢，导致 `axios` 变量未定义

## 解决方案
将所有 axios 调用替换为浏览器原生 fetch API

## 修改内容

### templates/index.html
- 移除 axios CDN 引用，改为注释说明
- 替换 9 个函数中的 axios 调用:
  - `reloadStats`: `axios.get()` → `fetch().then(r => r.json())`
  - `uploadStatsFile`: `axios.post(formData)` → `fetch({method:'POST', body:formData})`
  - `loadGuandu`: `axios.get(params)` → `fetch(url + '?' + URLSearchParams)`
  - `uploadGuanduFile`: `axios.post(formData)` → `fetch({method:'POST', body:formData})`
  - `doAutoMatch`: `axios.post(json)` → `fetch({method:'POST', headers, body:JSON.stringify()})`
  - `matchMembers`: 同上
  - `runAssignment`: 同上
  - `exportExcel`: 同上
  - `init`: `axios.get(params)` → `fetch(url + '?' + encodeURIComponent())`

- 更新版本标记: `v20260426-2100 fetch`

## 服务状态
- Flask 重启后运行于 PID 79705
- 端口: 5001

## 待验证
用户刷新页面后测试各功能是否正常工作
