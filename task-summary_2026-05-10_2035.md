# 任务总结：页面架构重构 — 主页+双模块

## 目标
将原来的5步向导改为"主页 + 双模块入口"架构

## 完成内容

### 新架构
- **主页**：显示两个模块入口卡片
  - ⚔️ 官渡人员分配（4步向导：导入→匹配→配置→导出）
  - 📋 考勤管理（3个Tab：成员名单/战报记录/创建战报）
- **子模块**：顶部有"← 返回主页"按钮 + 模块标题
- 分配模块保留步骤条（4步），考勤模块保留Tab切换

### 技术变更
- 新增 `currentModule` ref：'home' | 'assign' | 'attendance'
- `steps` 重命名为 `assignSteps`（4项，移除"考勤管理"）
- 步骤0-3的card加 `v-if="currentModule==='assign'"`
- 步骤4的card改为 `v-if="currentModule==='attendance'"`
- 考勤数据加载改为点击模块卡片时触发
- `clearAllData` 重置时回到主页
- 标题改为"官渡军团管理系统"

### CSS
- 新增 `.module-card` 样式：白色圆角卡片、hover上浮+金色阴影、图标/标题/描述

### 文件
- templates/index.html（修改）
- app.py（无改动）
