# 任务摘要 2026-04-24 15:12

## 目标
修复官渡自动分配工具前端三个问题

## 问题与修复

### 1. 成员名单列表不显示排序属性值
- **问题**: 成员 chip 固定显示 `getHp(m)`，只展示步兵生命值
- **修复**: 新增 `getSortValue(name)` 函数，根据当前 `sortBy`（hp/total/power）动态返回对应属性值；成员 chip 改用 `getSortValue(m)` 并带颜色标注

### 2. 未匹配成员待确认列表消失
- **问题**: 未匹配成员只在弹窗中展示，关闭弹窗后看不到；`doAutoMatch` 可能未被正确调用（服务端日志显示页面加载时无 match_members POST 请求）
- **修复**: 
  - 步骤2卡片中**始终内联展示**未匹配成员列表（不依赖弹窗），每个成员配下拉选择框
  - 新增 `matchDone` 标志，改善匹配状态显示
  - `confirmMatches` 中跳过的成员标记为 null（参与分配但 HP=0）
  - `doAutoMatch` 增加 console.log 调试日志

### 3. 点击"执行分配"无反应
- **问题**: 后端 API 正常（curl 测试 200），但前端可能吞掉了错误
- **修复**: 
  - `runAssignment` 增加 `alert()` 错误反馈（不再静默失败）
  - 增加前置校验（statsLoaded/members 为空时 alert 提示）
  - 增加详细 console.log 调试日志

## 关键文件
- `templates/index.html` — 前端 Vue 3 单文件，所有修改在此
- `app.py` — 后端未改动

## 后续
- 如"执行分配"仍不工作，需用户打开 F12 控制台截图红色报错
- 3个未匹配成员：花开不开花、轮回、冰弦
