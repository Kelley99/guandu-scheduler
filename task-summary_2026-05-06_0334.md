# 属性收集工具 - 12属性字段修复

## 目标
将属性收集工具从10个属性字段扩展到完整的12个（3兵种 × 4属性），并修正六维统计只取其中6个字段。

## 修改内容

### 12个属性字段（完整列表）
- 步兵：攻击力、防御力、生命值、破坏力
- 骑兵：攻击力、防御力、生命值、破坏力
- 弓兵：攻击力、防御力、生命值、破坏力

### 六维统计字段（仅6个）
步兵防御力 + 步兵生命值 + 骑兵攻击力 + 骑兵破坏力 + 弓兵攻击力 + 弓兵破坏力

### 缺失字段
本次新增：`cavalry_hp`（骑兵生命值）、`archer_hp`（弓兵生命值）

### 修改文件
1. **app.py**：attr_labels(12项)、UPDATE SQL(+2)、INSERT SQL(+2)、columns列表、导出headers(+2)、导出g()调用(+2)
2. **templates/index.html**：formData(+2)、HTML输入框(+2)、total6d computed(改为仅6字段)、resetForm(+2)
3. **数据库**：ALTER TABLE 添加 cavalry_hp、archer_hp 列

### 验证
- Python语法检查通过
- Flask重启成功(PID 5221, port 5002)
- /api/list 返回包含 cavalry_hp、archer_hp（共23个字段）
