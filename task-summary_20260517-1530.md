# 任务总结：考勤分工安排关联功能

**时间**: 2026-05-17 15:30 GMT+8  
**需求来源**: Kelley — 「缺席需要和分工安排表对应上」

---

## 问题根因

OCR 识别「没有记录到任何结果」的根本原因：

EasyOCR `readtext()` 返回的是**按文字块分割**的结果（如「1」「张三」「12345」是三个独立元素），后端把每个块当一行塞进 `lines` 列表，前端按整行解析当然匹配不上。

---

## 修复 1：OCR 后端结构化返回

**文件**: `app.py` → `/api/ocr` 路由（约第 1605 行）

改动：
1. 按 Y 坐标合并文字块成「行」（Y 差 < 行高阈值视为同行）
2. 每行按 X 坐标排序，拼接成完整行文字
3. 正则提取 `姓名` + `积分`（积分 = 行尾 4-12 位连续数字，去掉行首排名）
4. 返回 `{success: true, records: [{name, points}], text: "原始OCR文字"}`

**验证**:
```python
# 测试 API
curl -X POST -F "file=@test.jpg" http://127.0.0.1:5001/api/ocr
# 返回 records 而非 lines
```

---

## 修复 2：OCR 前端确认流程

**文件**: `templates/index.html`

新增/改动：
- `onOcrImage()` → 改用 `json.records` 而非 `json.lines`
- 同名成员积分取**最大值**（多张截图合并时）
- 上传后出现**确认表格**：可勾选、可点击编辑姓名/积分、可删除
- 默认全选，`toggleAllOcr()` 控制全选/取消
- `createReport()` 只提交 `selected=true` 的记录
- 新增 `attOcrSelected`、`attOcrEditing` ref

---

## 新功能：分工安排保存（两个入口）

### 入口 1：分配工具 → 一键保存

**位置**: 分配结果页（步骤3）→ 「💾 保存为分工安排」按钮（在「导出Excel」旁边）

**逻辑**:
1. 点击按钮 → 弹出 `showAssignmentModal`
2. 选择/输入军团名称（`sections` 列表 + 「新建...」选项）
3. 从 `teamResults.value` 提取所有队伍成员
4. 角色自动判断：`idx===0` → 队长；`pos.startsWith('A')` → A组；`pos.startsWith('B')` → B组；其余 → 成员
5. POST `/api/attendance/assignments/save`

### 入口 2：考勤模块 → 保存当前成员

**位置**: 考勤 → 成员名单 Tab → 「💾 保存为分工安排」按钮（在「从属性表导入」旁边）

**逻辑**:
1. 点击按钮 → 弹出同样的弹窗
2. 选择/输入军团名称
3. 把 `attMembers` 全部存为角色「成员」
4. POST `/api/attendance/assignments/save`

---

## 新功能：分工安排关联战报

**位置**: 考勤 → 创建战报 Tab → 「关联分工安排」下拉框

**逻辑**:
1. 前端 `loadAttAssignments()` 加载所有已保存的分工安排
2. 新建战报时选择对应的分工安排（`assignment_id` 字段）
3. 后端 `/api/attendance/reports/create` 收到 `assignment_id` 后：
   - OCR 识别到的成员 → 出席
   - 分工安排里有但 OCR 没识别到 → **缺席**
   - （可扩展：请假人员需另外标记）

**当前状态**: 分工安排列表默认空，需要先通过上面两个入口保存数据才会显示。

---

## 弹窗 UI

**文件**: `templates/index.html` → 插入在 `</body>` 前

- 弹窗 HTML：`showAssignmentModal` 控制显隐
- 下拉选择现有军团 / 选「新建...」输入新名称
- 根据来源（`assign` / `attendance`）显示不同提示文字
- 确认后调用 `confirmSaveAssignment()`
- 保存成功后自动刷新 `attAssignments` 列表

**CSS**: `.modal-overlay`、`.modal-content`、`.btn-warning` 样式已加入 `<style>`

---

## 后端 API 验证

```python
# 测试保存
POST /api/attendance/assignments/save
{
  "section": "测试军团",
  "members": [
    {"name": "张三", "role": "队长", "position": 0},
    {"name": "李四", "role": "A组", "position": 1}
  ]
}
→ {assignment_id: 1, success: True}

# 测试读取
GET /api/attendance/assignments
→ {success: True, data: [{id: 1, section: "测试军团", member_count: {...}}]}
```

测试数据已在验证后清理。

---

## 文件清单

| 文件 | 改动类型 |
|------|----------|
| `/Users/kelley/.qclaw/workspace-agent-29b6e205/app.py` | 修改 `/api/ocr` 路由 |
| `/Users/kelley/.qclaw/workspace-agent-29b6e205/templates/index.html` | 新增弹窗、按钮、JS函数 |

---

## 待 Kelley 验证

1. 上传积分榜截图 → 是否能正确识别姓名+积分 → 确认表格是否正常显示
2. 分配工具执行分配 → 点「保存为分工安排」→ 是否能成功保存
3. 考勤模块 → 点「保存为分工安排」→ 是否能成功保存
4. 创建战报时选择分工安排 → 缺席人员是否正确标记

---

## 已知限制

- 考勤成员保存为分工安排时，所有人角色都是「成员」（无细分）
- 如果分配工具的 `team.members` 里 `pos` 字段格式有变，角色判断可能需要调整
- 分工安排保存后用「新建战报」关联，但「修改分工安排」功能尚未做（目前是同名覆盖）

---

**任务状态**: ✅ 代码完成，等待 Kelley 端到端测试
