# 考勤战报系统优化（2026-05-11 01:36 UTC）

## 本轮优化内容

### 1. 数据库修复
- `attendance_detail` 表缺失 `points` 列（历史数据）- 手动执行 `ALTER TABLE` 修复

### 2. Excel 导出增加积分列
- 标题行：`['姓名', '状态', '功勋积分', '更新时间']`
- 数据行：`r['points'] or 0`
- 统计区新增：`['功勋积分合计', total_pts]`
- 列宽 A-D 各 18

### 3. 战报详情弹窗头部显示总积分
- 标题：`{{ section }} {{ date }} 战报 （总积分：XXX）`

### 4. 同名去重（最新值覆盖）
- 前端：`nameMap.set(name, pts)` 覆盖
- 后端：`pts_map[name] = pts` 赋值

### 待关注
- attribute_collector (5002) 集结加成/出征容量 OCR 解析 Bug（未修，需用户确认）
